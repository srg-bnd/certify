en:
  api:
    domains:
      index:
        desc: List of domains states
      create:
        desc: Add a tracking domain
      track:
        desc: Start tracking
      untrack:
        desc: Stop tracking
      destroy:
        desc: Remove the domain
